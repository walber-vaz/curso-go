package main

import "fmt"

func main() {
	// Utilizando o operador curto de declaração, atribua estes valores às variáveis
	// com os indetificadores "x", "y", "z"

	// Opos demostre os valores nesta variaveis, como
	// Uma única declaração print
	// Mútiplas declaração print
	x := 42
	y := "Jamed Bond"
	z := true

	fmt.Println(x, y, z)
	fmt.Println(x)
	fmt.Println(y)
	fmt.Println(z)
}
